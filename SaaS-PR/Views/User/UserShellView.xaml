<UserControl x:Class="SaaS_PR.Views.User.UserShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:user="clr-namespace:SaaS_PR.ViewModels.User"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=user:UserShellViewModel}"
             Background="{StaticResource AppBackground}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="1" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Grid Grid.Column="0" Background="{StaticResource SidebarBackground}">
            <Grid.RowDefinitions>
                <RowDefinition Height="64" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Logo -->
            <Border Grid.Row="0"
                    BorderBrush="{StaticResource DividerColor}"
                    BorderThickness="0 0 0 1">
                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Margin="16 0">
                    <materialDesign:PackIcon Kind="CloudOutline"
                                             Width="22" Height="22"
                                             Foreground="{StaticResource AccentColor}"
                                             VerticalAlignment="Center" />
                    <TextBlock Text="SaaS Manager"
                               FontSize="14"
                               FontWeight="Medium"
                               Foreground="{StaticResource TextPrimary}"
                               VerticalAlignment="Center"
                               Margin="10 0 0 0" />
                </StackPanel>
            </Border>
            
            <!-- Navigation history buttons -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="8 8" HorizontalAlignment="Center">
                <Button Style="{StaticResource MaterialDesignIconButton}"
                        Command="{Binding GoBackCommand}"
                        ToolTip="Назад"
                        IsEnabled="{Binding ShellNav.CanGoBack}"
                        Foreground="{StaticResource TextSecondary}">
                    <materialDesign:PackIcon Kind="ArrowLeft" Width="18" Height="18" />
                </Button>
                <Button Style="{StaticResource MaterialDesignIconButton}"
                        Command="{Binding GoForwardCommand}"
                        ToolTip="Вперед"
                        IsEnabled="{Binding ShellNav.CanGoForward}"
                        Foreground="{StaticResource TextSecondary}"
                        Margin="4 0 0 0">
                    <materialDesign:PackIcon Kind="ArrowRight" Width="18" Height="18" />
                </Button>
            </StackPanel>
            
            <!-- Navigation items -->
            <StackPanel Grid.Row="2" Margin="8 16 8 0">

                <TextBlock Text="ОГЛЯД"
                           Style="{StaticResource SectionLabel}"
                           Margin="8 0 0 6" />

                <Button Style="{StaticResource NavButton}"
                        Command="{Binding GoToDashboardCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ViewDashboardOutline"
                                                 Width="18" Height="18"
                                                 VerticalAlignment="Center" />
                        <TextBlock Text="Дашборд"
                                   VerticalAlignment="Center"
                                   Margin="12 0 0 0" />
                    </StackPanel>
                </Button>

                <TextBlock Text="ПІДПИСКИ"
                           Style="{StaticResource SectionLabel}"
                           Margin="8 16 0 6" />

                <Button Style="{StaticResource NavButton}"
                        Command="{Binding GoToBrowsePlansCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="PackageVariantClosedPlus"
                                                 Width="18" Height="18"
                                                 VerticalAlignment="Center" />
                        <TextBlock Text="Переглянути плани"
                                   VerticalAlignment="Center"
                                   Margin="12 0 0 0" />
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource NavButton}"
                        Command="{Binding GoToMySubscriptionCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ClipboardCheckOutline"
                                                 Width="18" Height="18"
                                                 VerticalAlignment="Center" />
                        <TextBlock Text="Мої підписки"
                                   VerticalAlignment="Center"
                                   Margin="12 0 0 0" />
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource NavButton}"
                        Command="{Binding GoToMyPaymentsCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CreditCardOutline"
                                                 Width="18" Height="18"
                                                 VerticalAlignment="Center" />
                        <TextBlock Text="Мої оплати"
                                   VerticalAlignment="Center"
                                   Margin="12 0 0 0" />
                    </StackPanel>
                </Button>

                <TextBlock Text="ОБЛІКОВИЙ ЗАПИС"
                           Style="{StaticResource SectionLabel}"
                           Margin="8 16 0 6" />

                <Button Style="{StaticResource NavButton}"
                        Command="{Binding GoToProfileCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AccountCircleOutline"
                                                 Width="18" Height="18"
                                                 VerticalAlignment="Center" />
                        <TextBlock Text="Профіль"
                                   VerticalAlignment="Center"
                                   Margin="12 0 0 0" />
                    </StackPanel>
                </Button>

            </StackPanel>

            <!-- User footer + logout -->
            <Border Grid.Row="3"
                    BorderBrush="{StaticResource DividerColor}"
                    BorderThickness="0 1 0 0"
                    Padding="16 12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding CurrentUserName}"
                                   FontSize="13"
                                   FontWeight="Medium"
                                   Foreground="{StaticResource TextPrimary}"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="Підписник"
                                   FontSize="11"
                                   Foreground="{StaticResource AccentColor}" />
                    </StackPanel>

                    <Button Grid.Column="1"
                            Style="{StaticResource MaterialDesignIconButton}"
                            Command="{Binding LogoutCommand}"
                            ToolTip="Вийти"
                            Foreground="{StaticResource TextSecondary}">
                        <materialDesign:PackIcon Kind="LogoutVariant" Width="18" Height="18" />
                    </Button>
                </Grid>
            </Border>

        </Grid>

        <!-- Vertical divider -->
        <Rectangle Grid.Column="1"
                   Fill="{StaticResource DividerColor}" />

        <!-- Main content area -->
        <ContentControl Grid.Column="2"
                        Content="{Binding ShellNav.CurrentView}"
                        ContentTemplateSelector="{Binding ViewLocator, RelativeSource={RelativeSource AncestorType=Window}}" />

    </Grid>
</UserControl>
