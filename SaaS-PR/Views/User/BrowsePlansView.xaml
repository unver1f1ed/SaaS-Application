<UserControl x:Class="SaaS_PR.Views.User.BrowsePlansView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:user="clr-namespace:SaaS_PR.ViewModels.User"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=user:BrowsePlansViewModel}"
             Background="{StaticResource AppBackground}">

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0 0 0 32">
            <TextBlock Text="Огляд планів"
                       Style="{StaticResource PageTitle}"
                       Margin="0" />
            <TextBlock Text="Оберіть план, що підходить вашим потребам"
                       Foreground="{StaticResource TextSecondary}"
                       FontSize="13" />
        </StackPanel>

        <!-- Plans grid -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Plans}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Style="{StaticResource Card}"
                                              Width="280"
                                              Margin="0 0 16 16">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <!-- Plan type badge -->
                                <Border Grid.Row="0"
                                        Background="#1A4DD0E1"
                                        CornerRadius="4"
                                        Padding="8 3"
                                        HorizontalAlignment="Left"
                                        Margin="0 0 0 12">
                                    <TextBlock Text="{Binding PlanType}"
                                               FontSize="10"
                                               FontWeight="Medium"
                                               Foreground="{StaticResource AccentColor}" />
                                </Border>

                                <!-- Plan name + price -->
                                <TextBlock Grid.Row="1"
                                           Text="{Binding Name}"
                                           FontSize="18"
                                           FontWeight="Medium"
                                           Foreground="{StaticResource TextPrimary}"
                                           Margin="0 0 0 4" />

                                <!-- Description -->
                                <TextBlock Grid.Row="2"
                                           Text="{Binding Description}"
                                           FontSize="13"
                                           Foreground="{StaticResource TextSecondary}"
                                           TextWrapping="Wrap"
                                           Margin="0 0 0 16" />

                                <!-- Pricing info -->
                                <StackPanel Grid.Row="3" Margin="0 0 0 16">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding BasePrice, StringFormat={}{0:C}}"
                                                   FontSize="26"
                                                   FontWeight="Light"
                                                   Foreground="{StaticResource TextPrimary}" />
                                        <TextBlock Text="{Binding BillingInterval, StringFormat=' / {0}'}"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextSecondary}"
                                                   VerticalAlignment="Bottom"
                                                   Margin="4 0 0 4" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal"
                                                Visibility="{Binding TrialDays, Converter={StaticResource NullToVisibilityConverter}}"
                                                Margin="0 4 0 0">
                                        <materialDesign:PackIcon Kind="ClockOutline"
                                                                 Width="14" Height="14"
                                                                 Foreground="{StaticResource AccentColor}"
                                                                 VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding TrialDays, StringFormat=' {0}-day free trial'}"
                                                   FontSize="12"
                                                   Foreground="{StaticResource AccentColor}"
                                                   VerticalAlignment="Center" />
                                    </StackPanel>
                                </StackPanel>

                                <!-- Subscribe button -->
                                <Button Grid.Row="4"
                                        Style="{StaticResource PrimaryButton}"
                                        Command="{Binding DataContext.SubscribeCommand,
                                                  RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}"
                                        Content="ПІДПИСАТИСЯ"
                                        HorizontalAlignment="Stretch" />

                            </Grid>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Feedback + loading -->
        <StackPanel Grid.Row="2" Margin="0 12 0 0">
            <TextBlock Text="{Binding SuccessMessage}"
                       Style="{StaticResource SuccessText}"
                       Visibility="{Binding SuccessMessage, Converter={StaticResource NullToVisibilityConverter}}" />
            <TextBlock Text="{Binding ErrorMessage}"
                       Style="{StaticResource ErrorText}"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}" />
            <ProgressBar Style="{StaticResource MaterialDesignLinearProgressBar}"
                         IsIndeterminate="True"
                         Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </StackPanel>

    </Grid>
</UserControl>
