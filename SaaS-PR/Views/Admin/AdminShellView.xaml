<UserControl x:Class="SaaS_PR.Views.Admin.AdminShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:admin="clr-namespace:SaaS_PR.ViewModels.Admin"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=admin:AdminShellViewModel}"
             Background="{StaticResource AppBackground}">

    <Grid>
        <Grid.ColumnDefinitions>
            <!-- Sidebar -->
            <ColumnDefinition Width="220" />
            <!-- Divider -->
            <ColumnDefinition Width="1" />
            <!-- Main content -->
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Grid Grid.Column="0" Background="{StaticResource SidebarBackground}">
            <Grid.RowDefinitions>
                <!-- Logo area -->
                <RowDefinition Height="64" />
                <!-- Navigation history buttons -->
                <RowDefinition Height="Auto" />
                <!-- Nav items -->
                <RowDefinition Height="*" />
                <!-- User footer -->
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Logo -->
            <Border Grid.Row="0"
                    BorderBrush="{StaticResource DividerColor}"
                    BorderThickness="0 0 0 1">
                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Margin="16 0">
                    <materialDesign:PackIcon Kind="CloudOutline"
                                             Width="22" Height="22"
                                             Foreground="{StaticResource AccentColor}"
                                             VerticalAlignment="Center" />
                    <TextBlock Text="SaaS Manager"
                               FontSize="14"
                               FontWeight="Medium"
                               Foreground="{StaticResource TextPrimary}"
                               VerticalAlignment="Center"
                               Margin="10 0 0 0" />
                </StackPanel>
            </Border>

            <!-- Navigation history buttons -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="8 8">
                <Button Style="{StaticResource MaterialDesignIconButton}"
                        Command="{Binding GoBackCommand}"
                        ToolTip="Назад"
                        IsEnabled="{Binding ShellNav.CanGoBack}"
                        Foreground="{StaticResource TextSecondary}">
                    <materialDesign:PackIcon Kind="ArrowLeft" Width="18" Height="18" />
                </Button>
                <Button Style="{StaticResource MaterialDesignIconButton}"
                        Command="{Binding GoForwardCommand}"
                        ToolTip="Вперед"
                        IsEnabled="{Binding ShellNav.CanGoForward}"
                        Foreground="{StaticResource TextSecondary}"
                        Margin="4 0 0 0">
                    <materialDesign:PackIcon Kind="ArrowRight" Width="18" Height="18" />
                </Button>
            </StackPanel>

            <!-- Navigation items -->
            <StackPanel Grid.Row="2" Margin="8 16 8 0">

                <TextBlock Text="ОГЛЯД"
                           Style="{StaticResource SectionLabel}"
                           Margin="8 0 0 6" />

                <Button Style="{StaticResource NavButton}"
                        Command="{Binding GoToDashboardCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ViewDashboardOutline"
                                                 Width="18" Height="18"
                                                 VerticalAlignment="Center" />
                        <TextBlock Text="Дашборд"
                                   VerticalAlignment="Center"
                                   Margin="12 0 0 0" />
                    </StackPanel>
                </Button>

                <TextBlock Text="MANAGEMENT"
                           Style="{StaticResource SectionLabel}"
                           Margin="8 16 0 6" />

                <Button Style="{StaticResource NavButton}"
                        Command="{Binding GoToUsersCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AccountMultipleOutline"
                                                 Width="18" Height="18"
                                                 VerticalAlignment="Center" />
                        <TextBlock Text="Користувачі"
                                   VerticalAlignment="Center"
                                   Margin="12 0 0 0" />
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource NavButton}"
                        Command="{Binding GoToPlansCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="PackageVariantClosed"
                                                 Width="18" Height="18"
                                                 VerticalAlignment="Center" />
                        <TextBlock Text="Плани"
                                   VerticalAlignment="Center"
                                   Margin="12 0 0 0" />
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource NavButton}"
                        Command="{Binding GoToPlanAddonsCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="PuzzleOutline"
                                                 Width="18" Height="18"
                                                 VerticalAlignment="Center" />
                        <TextBlock Text="Додатки планів"
                                   VerticalAlignment="Center"
                                   Margin="12 0 0 0" />
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource NavButton}"
                        Command="{Binding GoToSubscriptionsCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ClipboardListOutline"
                                                 Width="18" Height="18"
                                                 VerticalAlignment="Center" />
                        <TextBlock Text="Підписки"
                                   VerticalAlignment="Center"
                                   Margin="12 0 0 0" />
                    </StackPanel>
                </Button>
                
                <Button Style="{StaticResource NavButton}"
                        Command="{Binding GoToSubscriptionAddonsCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="PuzzlePlusOutline"
                                                 Width="18" Height="18"
                                                 VerticalAlignment="Center" />
                        <TextBlock Text="Додатки підписок"
                                   VerticalAlignment="Center"
                                   Margin="12 0 0 0" />
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource NavButton}"
                        Command="{Binding GoToPaymentsCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CreditCardOutline"
                                                 Width="18" Height="18"
                                                 VerticalAlignment="Center" />
                        <TextBlock Text="Оплати"
                                   VerticalAlignment="Center"
                                   Margin="12 0 0 0" />
                    </StackPanel>
                </Button>

                <TextBlock Text="ОБЛІКОВИЙ ЗАПИС"
                           Style="{StaticResource SectionLabel}"
                           Margin="8 16 0 6" />

                <Button Style="{StaticResource NavButton}"
                        Command="{Binding GoToProfileCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AccountCircleOutline"
                                                 Width="18" Height="18"
                                                 VerticalAlignment="Center" />
                        <TextBlock Text="Профіль"
                                   VerticalAlignment="Center"
                                   Margin="12 0 0 0" />
                    </StackPanel>
                </Button>

            </StackPanel>

            <!-- User footer + logout -->
            <Border Grid.Row="4"
                    BorderBrush="{StaticResource DividerColor}"
                    BorderThickness="0 1 0 0"
                    Padding="16 12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding CurrentUserName}"
                                   FontSize="13"
                                   FontWeight="Medium"
                                   Foreground="{StaticResource TextPrimary}"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="Адміністратор"
                                   FontSize="11"
                                   Foreground="{StaticResource AccentColor}" />
                    </StackPanel>

                    <Button Grid.Column="1"
                            Style="{StaticResource MaterialDesignIconButton}"
                            Command="{Binding LogoutCommand}"
                            ToolTip="Вийти"
                            Foreground="{StaticResource TextSecondary}">
                        <materialDesign:PackIcon Kind="LogoutVariant" Width="18" Height="18" />
                    </Button>
                </Grid>
            </Border>

        </Grid>

        <!-- Vertical divider -->
        <Rectangle Grid.Column="1"
                   Fill="{StaticResource DividerColor}" />

        <!-- Main content area — driven by ShellNavigationService -->
        <ContentControl Grid.Column="2"
                        Content="{Binding ShellNav.CurrentView}"
                        ContentTemplateSelector="{Binding ViewLocator, RelativeSource={RelativeSource AncestorType=Window}}" />

    </Grid>
</UserControl>
