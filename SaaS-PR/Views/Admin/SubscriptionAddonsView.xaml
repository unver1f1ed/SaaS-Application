<UserControl x:Class="SaaS_PR.Views.Admin.SubscriptionAddonsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:admin="clr-namespace:SaaS_PR.ViewModels.Admin"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=admin:SubscriptionAddonsViewModel}"
             Background="{StaticResource AppBackground}">

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0 0 0 24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Додатки підписок" Style="{StaticResource PageTitle}" Margin="0" />
                <TextBlock Text="Керуйте додатками підписок користувачів"
                           Foreground="{StaticResource TextSecondary}" FontSize="13" />
            </StackPanel>
            <Button Grid.Column="1"
                    Style="{StaticResource SecondaryButton}"
                    Command="{Binding LoadUsersCommand}"
                    VerticalAlignment="Top">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Refresh" Width="16" Height="16" VerticalAlignment="Center" />
                    <TextBlock Text="Оновити" Margin="8 0 0 0" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </Grid>

        <!-- User picker and filters -->
        <Grid Grid.Row="1" Margin="0 0 0 12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="200" />
            </Grid.ColumnDefinitions>
            
            <ComboBox Grid.Column="0"
                      materialDesign:HintAssist.Hint="Оберіть користувача"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}"
                      ItemsSource="{Binding Users}"
                      SelectedItem="{Binding SelectedUser}"
                      Foreground="{StaticResource TextPrimary}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>
                            <Run Text="{Binding FirstName}" />
                            <Run Text="{Binding LastName}" />
                            <Run Text=" — " />
                            <Run Text="{Binding Email}" Foreground="{StaticResource TextSecondary}" />
                        </TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <TextBox Grid.Column="2"
                     materialDesign:HintAssist.Hint="Пошук додатків..."
                     materialDesign:TextFieldAssist.LeadingIcon="Magnify"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Foreground="{StaticResource TextPrimary}" />
        </Grid>

        <!-- Subscription picker -->
        <ComboBox Grid.Row="2"
                  materialDesign:HintAssist.Hint="Оберіть підписку"
                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                  ItemsSource="{Binding Subscriptions}"
                  SelectedItem="{Binding SelectedSubscription}"
                  DisplayMemberPath="PlanName"
                  Foreground="{StaticResource TextPrimary}"
                  Margin="0 0 0 16" />

        <!-- Content -->
        <Grid Grid.Row="3"
              Visibility="{Binding SelectedSubscription, Converter={StaticResource NullToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="320" />
            </Grid.ColumnDefinitions>

            <!-- Current addons list -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0"
                           Text="АКТИВНІ ДОДАТКИ"
                           Style="{StaticResource SectionLabel}"
                           Margin="0 0 0 12" />
                <materialDesign:Card Grid.Row="1" Style="{StaticResource Card}" Padding="0">
                    <DataGrid ItemsSource="{Binding CurrentAddons}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              Background="Transparent"
                              BorderThickness="0"
                              RowBackground="Transparent"
                              AlternatingRowBackground="#0AFFFFFF"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="{StaticResource DividerColor}"
                              Foreground="{StaticResource TextPrimary}"
                              materialDesign:DataGridAssist.CellPadding="16 12"
                              materialDesign:DataGridAssist.ColumnHeaderPadding="16 12">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ДОДАТОК" Binding="{Binding AddonName}" Width="*" />
                            <DataGridTextColumn Header="К-СТЬ" Binding="{Binding Quantity}" Width="Auto" />
                            <DataGridTextColumn Header="ДОДАНО"
                                                Binding="{Binding AddedDate, StringFormat=dd/MM/yyyy}"
                                                Width="Auto" />
                            <DataGridTemplateColumn Header="" Width="60">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                                Command="{Binding DataContext.RemoveAddonCommand,
                                                          RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                ToolTip="Прибрати додаток"
                                                Foreground="{StaticResource ErrorColor}">
                                            <materialDesign:PackIcon Kind="MinusCircleOutline" Width="18" Height="18" />
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </materialDesign:Card>
            </Grid>

            <!-- Add addon panel -->
            <materialDesign:Card Grid.Column="2" Style="{StaticResource Card}">
                <StackPanel>

                    <TextBlock Text="Додати додаток"
                               FontSize="16" FontWeight="Medium"
                               Foreground="{StaticResource TextPrimary}"
                               Margin="0 0 0 24" />

                    <TextBlock Text="ДОСТУПНІ ДОДАТКИ" Style="{StaticResource SectionLabel}" />

                    <ComboBox materialDesign:HintAssist.Hint="Оберіть додаток"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"
                              ItemsSource="{Binding AvailableAddons}"
                              SelectedItem="{Binding SelectedAvailableAddon}"
                              Foreground="{StaticResource TextPrimary}"
                              Margin="0 0 0 16">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding AddonName}" FontWeight="Medium" />
                                    <TextBlock Foreground="{StaticResource TextSecondary}" FontSize="11">
                                        <Run Text="{Binding UnitPrice, StringFormat={}{0:C}}" />
                                        <Run Text=" / " />
                                        <Run Text="{Binding BillingType}" />
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <TextBox materialDesign:HintAssist.Hint="Кількість"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Text="{Binding FormQuantity, UpdateSourceTrigger=PropertyChanged}"
                             Foreground="{StaticResource TextPrimary}"
                             Margin="0 0 0 24" />

                    <TextBlock Text="{Binding ErrorMessage}"
                               Style="{StaticResource ErrorText}"
                               Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"
                               Margin="0 0 0 16" />

                    <Button Style="{StaticResource PrimaryButton}"
                            Command="{Binding AddAddonCommand}"
                            HorizontalAlignment="Stretch">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="PlusCircleOutline" Width="16" Height="16"
                                                     VerticalAlignment="Center" />
                            <TextBlock Text="Додати додаток" Margin="8 0 0 0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>

                </StackPanel>
            </materialDesign:Card>

        </Grid>

        <!-- Loading -->
        <ProgressBar Grid.Row="4"
                     Style="{StaticResource MaterialDesignLinearProgressBar}"
                     IsIndeterminate="True"
                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                     Margin="0 12 0 0" />

    </Grid>
</UserControl>
